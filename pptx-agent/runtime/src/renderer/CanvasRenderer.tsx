import {useEffect,useRef} from 'react';
import type {Dict} from '../core/types';
export function CanvasRenderer({p}:{p:Dict}){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const ctx=ref.current?.getContext('2d');if(!ctx)return;ctx.clearRect(0,0,p.width,p.height);for(const item of p.items??[]){ctx.fillStyle=item.fill??'#2563eb';if(item.type==='circle'){ctx.beginPath();ctx.arc(item.x,item.y,item.radius,0,Math.PI*2);ctx.fill();}else if(item.type==='rect')ctx.fillRect(item.x,item.y,item.width,item.height);else throw new Error('Canvas supports explicit rect/circle collections');}},[p]);return <foreignObject width={p.width} height={p.height}><canvas ref={ref} width={p.width} height={p.height} aria-label={p.ariaLabel??'Data visualization'} style={{width:'100%',height:'100%'}}/></foreignObject>;}
