export function logicalPoint(clientX:number,clientY:number,rect:{left:number;top:number;width:number;height:number},viewport:{width:number;height:number}){const scale=Math.min(rect.width/viewport.width,rect.height/viewport.height);return{x:(clientX-rect.left-(rect.width-viewport.width*scale)/2)/scale,y:(clientY-rect.top-(rect.height-viewport.height*scale)/2)/scale};}
export function localPoint(x:number,y:number,node:{x?:number;y?:number;rotation?:number;scale?:number}){const a=-(node.rotation??0)*Math.PI/180;const dx=x-(node.x??0),dy=y-(node.y??0),s=node.scale??1;return{x:(dx*Math.cos(a)-dy*Math.sin(a))/s,y:(dx*Math.sin(a)+dy*Math.cos(a))/s};}
